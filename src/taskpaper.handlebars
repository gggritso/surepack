- pack for trip: @parallel(false) @context(Home, Chore)
  - Dopp: @parallel(false) @context(Home, Chore)
  {{#each dopp }}
    - {{this}} @context(Home, Chore)
  {{/each}}
  {{#if shavingKit.length}}
  - Shaving Kit @parallel(false) @context(Home, Chore)
  {{#each shavingKit }}
    - {{this}} @context(Home, Chore)
  {{/each}}
  {{/if}}
  - Backpack @parallel(false) @context(Home, Chore)
  {{#each backpack }}
    - {{this}} @context(Home, Chore)
  {{/each}}
  - Duffel @parallel(false) @context(Home, Chore)
  {{#each duffel }}
    - {{this}} @context(Home, Chore)
  {{/each}}
